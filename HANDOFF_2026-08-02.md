# 引き継ぎ 2026-08-02 — 身体化v2単発検証

## 安全なソース復元点

- branch: `minecolonies-experiment`
- 身体化追跡実装: `a692b4b`
- 作業開始時checkpoint: `8b01ce8`（origin / fork push済み）
- P1拡張前checkpoint: `4c9a7fc`
- 既存未追跡の`compare_metrics.js`、`ops.js`、`zone_audit.js`はユーザー所有として変更・stageしていない。

## 今回確認できた閉ループ

1. 自然な空腹者と関係者を選ぶ。
2. v2がprofile、現在の困窮、有向関係、距離、所持資源からhelp/refuseを決める。
3. helpなら助け手が実際に接近し、既存食料1個を6ブロック以内で渡す。
4. MineColonies市民が食べてsaturationを回復する。
5. append-only eventをobserverが一度だけ読み、trust、affinity、gratitude、resentment、obligation、記憶を更新する。

市民22→14はfish dinner 4→3 / 0→1、saturation 0→6.4、受け手視点trust 0.58、
gratitude 0.18、obligation 0.10まで確認済み。v2拒否5件では受け手視点trust 0.44、
affinity 0.47、resentment 0.16を確認した。

## 今回のコード変更

- `social_help_daemon.js`
  - 受け手が現在持っていない料理を優先する。
  - 250msごとに両者の実位置を確認し、2秒ごとに受け手の最新位置へ再標的化する。
  - 6ブロック以内になった瞬間に移転し、既定15秒で追跡を打ち切る。
- `supply_bot.js`
  - `SOCIAL_HELP_GRACE_MS`を追加。既定0なので従来の直接給餌を変えない。
  - 常駐社会支援と組み合わせる時だけ、最初の応答時間を市民間援助へ譲れる。
- 単体テストで追跡、再標的化、料理多様性、供給猶予を確認した。

修正版の高速ライブ試験は市民3→21、初期距離18.0ブロック、約0.56秒で成功した。
助け手X=492→507、受け手X=510、fish dinner 157→156 / 0→1。

## 現在のライブ状態

- 稼働: Forge/Bridge、`persona_daemon.js`、`social_observer.js`、`supply_bot.js`各1個。
- tickrate: adaptive governor有効。
- 非稼働: council、`social_help_daemon.js`常駐、social information常駐。
- supply botは`SOCIAL_HELP_GRACE_MS`なし、すなわち従来の即時安全網で稼働中。
- Restaurant sitting positionエラーは既知で継続。今回の社会支援変更とは独立。

Forgeの確認はホスト名前空間で行うこと。通常sandboxからのlocalhost/`ps`は不通・不可視に見える場合がある。
今回の再起動は前景PTYセッション上のJavaラッパーで安定し、`/ping`と実PIDをホスト側で確認した。

## 常駐化しなかった理由

単発試験は成功したが、v2常駐は市民移動と食料移転を継続的に繰り返す別の外部作用になる。
今回は常駐起動を行わず、供給猶予も無効へ戻した。実施時は次を一組にする。

```bash
SOCIAL_HELP_EXECUTE=true
SOCIAL_HELP_APPRAISAL_MODEL=v2
SOCIAL_HELP_DECISION_MODE=max
SOCIAL_HELP_NEED_BAND=hungry
SOCIAL_HELP_POLL_MS=5000
SOCIAL_HELP_PURSUIT_TIMEOUT_MS=15000
SOCIAL_HELP_GRACE_MS=20000  # supply_bot側
```

## 次の安全な順番

1. 18ブロック以外の距離帯を単発で測り、成功率・所要時間・本来の仕事への影響を記録する。
2. 常駐試験は時間上限、最大件数、停止条件を先に固定し、v2と供給猶予を同時に開始・停止する。
3. 比較条件を教員と確認後、v1/v2/関係なしの主要指標を事前登録する。
